# @ 和通知

`@登录名` 只在开主题和回帖时扫一遍。改帖不补扫。不存在的名字不当通知。自己 @ 自己不算。楼里 `@登录名` 链到 `/u/{login}`。

通知一张表、一个 `kind` 列。现在：`mention`、`reply`。以后私信、系统通知加 kind，不改表。存 `member_id` / `actor_id` / `thread_id` / `post_id`，不存 URL。

看不见就不写：停用版、一楼已隐藏的主题，会员收不到。顶栏「通知」带未读数。点一条标已读并去那一楼。
