# 隐藏帖

创始人/运营者在主题页对每一楼点「隐藏」或「取消隐藏」。隐藏不是删除，帖和楼号都还在。

- 中间楼：会员看见楼号、作者（显示名 + 登录名）和「已隐藏」，看不见正文。
- 一楼隐藏：整条主题从会员的版块列表消失。会员打开旧链接看到「这篇主题不可见」。创始人/运营者仍能进、能回、能再藏其他楼。
- 作者不能改已隐藏的帖。
- 藏帖不改变 `last_post_at`。

旧库：`ALTER TABLE posts ADD COLUMN hidden INTEGER NOT NULL DEFAULT 0`，列已存在则忽略。
