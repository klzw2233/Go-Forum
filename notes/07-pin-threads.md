# 置顶

创始人/运营者在版块列表对主题点「置顶 / 取消置顶」，置顶条可「上移 / 下移」。会员看见同样顺序，没有按钮。

置顶排在未置顶之上。未置顶仍按最后一帖时间。新置顶接在置顶区最下（后置顶的在下）。取消再置顶也当作新置顶。上移/下移与相邻置顶交换顺序。

一楼已隐藏的主题仍可置顶，只影响创始人/运营者看到的列表；会员侧该主题本来就不在列表里。

旧库：`ALTER TABLE threads ADD COLUMN pin_rank INTEGER NOT NULL DEFAULT 0`。
